- name: configure db server
  hosts: db
  tasks:
- name: install mysql 
    ansible.builtin.dnf:
      name: mysql
      state: installed

  - name: start and enable mysql
    ansible.builtin.service:
      name: mysqld
      state: started
      enabled: yes



  


  